#!/bin/sh
# -*- shell-script -*-

# Assume this project uses webpack-rails gem if it exposes the
# "webpack:compile" Rake task.
#
# `grep` will set the significant exit code accordingly.

echo "webpack-rails"

build_dir=$1

cd $build_dir

bundle exec rake -T | grep 'webpack:compile' >/dev/null
